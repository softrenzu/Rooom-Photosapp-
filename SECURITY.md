# Security policy

Please report security issues privately to `support@rooomtech.com`. Do not include Google tokens, Apple signing keys, passwords, private photos, or other secrets in a GitHub issue.

Supported release: 1.0.x.

The project intentionally uses the limited Google Drive `drive.file` scope, direct HTTPS transfers, local app-sandbox storage for queued photos, no developer backend, and no analytics or advertising SDK.
